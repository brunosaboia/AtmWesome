@model IEnumerable<Coinify.Web.Models.CoinSize>

@{
    ViewData["Title"] = "Index";
}

<h2>List Coin Sizes</h2>

<p>
    <a asp-action="Create">Create New Size</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Size)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Size)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CoinSizeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CoinSizeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CoinSizeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
